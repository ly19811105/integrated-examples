xx.yy { //修改成自己的域名
    root * /var/www
    file_server
    
    @shadowsocks_ws {
        path /ss1v2ray  //修改成自己的path
        header Connection *Upgrade*
        header Upgrade websocket
    }
    reverse_proxy @shadowsocks_ws 127.0.0.1:2004 //转发给本机v2ray对应端口
}
